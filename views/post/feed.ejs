<% posts.forEach(function (post) { %>
   <h3><a href="/posts/<%= post._id %>/view"> <%= post.title %></a> Score: <%=post.likes%></h3>
  <h4> <%= post.author %> </h4>
  <h5> <%= post.viewpoint %> </h5>
  <p>
    <%= post.content %>
  </p>
  <p>
    <%= post.comments.length %> comments
  </p>
  <p>
    <%= post.date %>
  </p>
<% }) %>
